Default CountersList component with all props:

```jsx
const counter = {
	number: 997,
	label: 'Police'
};
<CountersList counters={[counter, counter, counter]} />;
```

CountersList component with out props:

```jsx
<CountersList />
```
