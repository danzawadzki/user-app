Default orange heart icon with all props:

```jsx
<Icon
	name="heart"
	type="far"
	color="orange"
	onClick={() => alert(window.location.href)}
/>
```

Gray sha

```jsx
<Icon
	name="share-square"
	type="fas"
	color="gray"
	onClick={() => alert(window.location.href)}
/>
```
