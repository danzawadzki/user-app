Default counter component with all props:

```jsx
<Counter number={999} label="Label" />
```

Default counter component with out number prop (which is optional):

```jsx
<Counter label="Label" />
```
